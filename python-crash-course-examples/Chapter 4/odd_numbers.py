# Using the third argument in the range() function, create a list of odd numbers from 1 - 20. Then use a for loop to display the numbers of the list.
odd_nums = list(range(1, 21, 2))
for num in odd_nums:
	print(num)